fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
e2b-code-interpreter==1.0.5
langgraph==0.2.56
langchain==0.3.9
langchain-openai==0.2.11
langchain-core==0.3.21
sse-starlette==2.1.3
pydantic==2.10.3

# Persistence
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
pgvector==0.3.6

# Observability
langfuse>=2.50.0

# Tool layer — REST adapter
httpx==0.27.2
# MCP adapter (P1/future): langchain-mcp-adapters needs langchain-core>=0.3.36, which
# conflicts with the pinned 0.3.21 above. app/tools/adapters/mcp.py imports it lazily,
# so MCP tools only require it at call time. To enable MCP, bump the langchain stack
# (langchain-core>=0.3.36 + compatible langchain/langgraph/langchain-openai) and add:
#   langchain-mcp-adapters==0.1.0
